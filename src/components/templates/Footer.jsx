import "./Footer.css";
import React from "react";

const Footer = (props) => {
  return (
    <footer className="footer">
      Desenvolvido por Ped<strong>Rocker</strong>
    </footer>
  );
};

export default Footer;
